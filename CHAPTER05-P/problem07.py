s = {8, 7, "Nisha", [1,2]}
# we can't change the value in the set but we can remove the value by using a function and add another value by using add function.
# but we can't change it, cause sets are immutable.
## also we can't include a list in the set because lists are mutable and sets are immutable so we can't include a list in the set because it will change the set and it will not be able to be hashed and it will not be able to be added to the set.
print(s)

#to add the (1,2) together as an element we can use () bracket instead of [] as they represent lists