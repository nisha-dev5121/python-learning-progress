e = set()
#empty set
#sets and dictionaries both use {} as their syntax but the difference lies in the way they define their empty set
#for dictionary empty dictionary is just {} but for sets empty sets are defined using set() function.


s = {1, 5, 32, 5, 5, 5}
#no matter how many times a single digit is written in the set it's just gonna show once because sets only store unique values.
print(s)